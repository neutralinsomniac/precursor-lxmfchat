# Python deps for the build/flash/test scripts (usb_update.py, the interop and
# node harnesses). Create the venv with the nix shell's python:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Versions pinned to what the interop known-answer vectors were captured against.
rns==1.3.5
lxmf==1.0.1
pyusb==1.3.1
precursorupdater==0.1.5
