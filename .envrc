# direnv entrypoint — `direnv allow` once, then the dev environment (toolchain,
# X11/libusb on LD_LIBRARY_PATH, the Python venv) loads automatically on cd.
# See shell.nix. Install nix-direnv for caching if entry feels slow.
watch_file shell.nix
use nix
